# vscode-codemap-gcode
A gcode mapper which works with the vscode extension CodeMap.  CodeMap is an "Interactive code map for quick visualization and navigation within code DOM objects (e.g. classes, members)."
